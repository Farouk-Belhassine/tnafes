<form method="post" action="{{ path('categorie_event_delete', {'idCategEvent': categorie_event.idCategEvent}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_event.idCategEvent) }}">
    <button class="btn">Supprimer</button>
</form>
