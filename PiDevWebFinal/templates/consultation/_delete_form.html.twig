<form method="post" action="{{ path('consultation_delete', {'idConsultation': consultation.idConsultation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ consultation.idConsultation) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
