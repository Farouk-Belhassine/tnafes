

{% extends 'basedisplay.html.twig' %}

{% block titre %}
    Liste des abonnements
{% endblock %}
{% block table %}

    <table class="table">
        <tbody>
            <tr>
                <th>IdAbonnement</th>
                <td>{{ abonnement.idAbonnement }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ abonnement.dateDebut }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ abonnement.dateFin }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ abonnement.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('abonnement_index') }}">back to list</a>

    <a href="{{ path('abonnement_edit', {'idAbonnement': abonnement.idAbonnement}) }}">edit</a>

    {{ include('abonnement/_delete_form.html.twig') }}
{% endblock %}
